export const generateRandomNumber = () => Math.floor(Math.random() * 101);

export const upperCase = (phrase) => phrase.toUpperCase();

export const firstLetter = (phrase) => phrase[0];

export const concatenate = (firstWord, secondWord) => firstWord + secondWord;
